# ============================================================
# requirements.txt — StayKey
# Instalar: pip install -r requirements.txt
# ============================================================

# ── Framework ────────────────────────────────────────────────
Flask==3.1.0
Werkzeug==3.1.3

# ── Banco de dados ───────────────────────────────────────────
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
psycopg2-binary==2.9.9         # Driver PostgreSQL (binary = sem compilar)
SQLAlchemy==2.0.36

# ── Autenticação / OAuth ─────────────────────────────────────
Authlib==1.3.2
Flask-Login==0.6.3
requests==2.32.3

# ── Segurança ────────────────────────────────────────────────
Flask-WTF==1.2.2               # CSRF protection
python-dotenv==1.0.1           # Leitura do .env

# ── Servidor WSGI ────────────────────────────────────────────
gunicorn==22.0.0

# ── Utilitários ──────────────────────────────────────────────
python-dateutil==2.9.0
python-slugify==8.0.4          # slugify() usado em app/models/imovel.py

# ── Notificações push no navegador (Web Push / VAPID) ─────────
pywebpush==2.0.1

# ── Armazenamento persistente de arquivos (Cloudflare R2, S3-compatible) ──
# Sem as variáveis R2_* configuradas, o app cai pro disco local de sempre —
# mas o disco do Render é efêmero (uploads somem a cada redeploy), então em
# produção o R2 é o que faz fotos de imóvel/avatar/documentos "ficarem
# salvos pra sempre".
boto3==1.35.99

# ── Envio de e-mail via API HTTP (Resend) ─────────────────────
# Sem RESEND_API_KEY configurada, o app cai pro SMTP do Gmail de sempre —
# mas a Render bloqueia a porta SMTP de saída, então em produção o Resend
# é o que faz os e-mails (verificação, recuperação de senha etc.) chegarem.
resend==2.39.0
